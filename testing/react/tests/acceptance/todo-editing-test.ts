import puppeteer from 'puppeteer';

describe('Acceptance | todo editing', () => {
    test('the initial todo can be edited', () => {

    });

    test('the initial todo can have the text change', () => {

    });

    test('the initial todo can be completed', () => {

    });
});