# Ember

### Docker (recommended)
```bash
docker build . -t event-handling-ember
docker run -it --rm event-handling-ember
```

### Local
```bash
yarn install
yarn start
```

## File(s) of interest

- src/ui/components/*
